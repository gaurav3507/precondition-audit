# A100 node. The environment that produced every real-data artefact:
# results/descriptives/ and results/preprocessing/, including the eigenvalue
# sidecars.
#
#   CPython 3.10.12, Linux x86_64 (5.15.0-1065-nvidia), BLAS scipy-openblas
#
# Read off meta.versions.platform in
# results/descriptives/2026-08-11T06-12-06Z__k562.json and its siblings, not
# typed from memory.
#
#   python3.10 -m venv .venv && . .venv/bin/activate
#   pip install -r requirements-a100.txt
#
# THE TWO ENVIRONMENTS DIFFER AND THE DIFFERENCE IS LOAD-BEARING. numpy and
# scipy differ by a minor version and the BLAS backend differs outright
# (Accelerate on the Mac, scipy-openblas here). SVD-derived quantities depend on
# the backend, so cross-machine agreement is checked by
# 85_dataset_descriptives.py --selftest against pinned constants rather than
# assumed. Do not merge this file with requirements-mac.txt.

numpy==2.2.6
scipy==1.15.3
pandas==2.3.3
scikit-learn==1.7.2

# Raw-data loaders only. Frangieh and Replogle need no more than the above;
# Norman is read through anndata.
anndata==0.11.4
scanpy==1.11.5

# matplotlib is not pinned here: no figure is generated on the A100. Figures are
# rebuilt from committed artefacts on any machine, under requirements-mac.txt.
