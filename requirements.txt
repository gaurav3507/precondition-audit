# Pinned to the environment that produced every committed table and figure:
# CPython 3.11.15, Darwin arm64, BLAS accelerate.
#
# Restricted to packages actually imported by scripts/. This is not a freeze of
# the whole environment: a reproduction should fail on a version that matters,
# not on one that does not.
#
#   python3.11 -m venv .venv && . .venv/bin/activate
#   pip install -r requirements.txt

numpy==2.4.6
scipy==1.17.1
matplotlib==3.11.0
pandas==3.0.3

# ---------------------------------------------------------------- A100 only
# Imported only by the loaders that read raw data (40_screen_norman.py reads
# .h5ad through anndata). Nothing under "Anywhere" in docs/REPRODUCE.md needs
# it, and every table and figure in paper/ was built without it.
#
# DELIBERATELY UNPINNED. anndata is not installed in the environment that
# produced the committed artefacts, so there is no version here to record.
# Pin it on the machine that holds the data, and write the pin back here.
anndata
